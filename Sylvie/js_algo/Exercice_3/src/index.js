function range(firstNumber, endNumber) {
  // Code the function here.
  if (firstNumber < endNumber) {
    // return croissant
    return tab;
  }
  if (firstNumber > endNumber) {
    // return décroissant
    return tab;
  }
}

range(1, 6);
// Do not remove last lines, it is for tests
module.exports = range;
