function range(numOne, numTwo) {
  const someSports = ["numOne", "numTwo"];

  for (let i = 0; i < tab.length + 1; i++) {
    console.log(tab[i]);
  }
}

// Do not remove last lines, it is for tests
module.exports = range;
