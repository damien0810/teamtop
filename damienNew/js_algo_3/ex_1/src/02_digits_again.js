// Create the famous `digits` array using a `for-loop`.
// ## Build an array
//
// Create the famous `digits` array using a `for-loop`.// ## Build an array
//
let digits = [];

for (let index = 0; index < 10; index++) {
  digits.push(index);
}

console.log(digits);
